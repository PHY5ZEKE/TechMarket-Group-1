@model TechMarket.Models.ToReceiveProduct

<h1>Products To Receive</h1>

@if (Model != null)
{
    <dl class="row">
        <dt class="col-sm-2">Product Name:</dt>
        <dd class="col-sm-10">@Model.ProductName</dd>

        <dt class="col-sm-2">Image:</dt>
        <dd class="col-sm-10">
            @if (!string.IsNullOrEmpty(Model.ProductImageURL))
            {
                <img src="~/@Model.ProductImageURL" alt="Product Image" style="max-width: 200px; max-height: 200px;" />
            }
            else
            {
                <span>No Image Available</span>
            }
        </dd>

        <dt class="col-sm-2">Description:</dt>
        <dd class="col-sm-10">@Model.ProductDescription</dd>

        <dt class="col-sm-2">Price:</dt>
        <dd class="col-sm-10">@Model.ProductPrice</dd>

        <dt class="col-sm-2">Purchase Date:</dt>
        <dd class="col-sm-10">@Model.PurchaseDate</dd>

        <dt class="col-sm-2">Seller Name:</dt>
        <dd class="col-sm-10">@Model.SellerName </dd>

        <dt class="col-sm-2">Seller Email:</dt>
        <dd class="col-sm-10">@Model.SellerEmail</dd>

        <dt class="col-sm-2">Seller Contact:</dt>
        <dd class="col-sm-10">@Model.SellerContact</dd>
    </dl>
}
else
{
    <p>No details available for the selected purchased product.</p>
}

<a asp-action="ToReceive" class="btn btn-primary">Back</a>
